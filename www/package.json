{
    "name": "plz2wk-monorepo",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "concurrently \"npm --prefix frontend run dev\" \"npm --prefix server run dev\""
    },
    "devDependencies": {
        "concurrently": "^9.1.2"
    }
}
